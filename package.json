{
  "name": "tazavesh",
  "version": "0.1.0",
  "description": "Tazavesh — AI Agent Gateway on Cloudflare Workers. Multi-platform bot hub with memory, tools, and skills.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "agents": "^0.12.3",
    "@cloudflare/ai-chat": "^0.6.2",
    "workers-ai-provider": "^0.2.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
